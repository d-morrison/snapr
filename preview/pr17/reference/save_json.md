# Save an R object to a JSON file

Save an R object to a JSON file

## Usage

``` r
save_json(x)
```

## Arguments

- x:

  An R object to save

## Value

[character](https://rdrr.io/r/base/character.html) Path to the temporary
JSON file
