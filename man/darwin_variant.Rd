% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/system_os.R
\name{darwin_variant}
\alias{darwin_variant}
\title{Get darwin snapshot variant for macOS}
\usage{
darwin_variant()
}
\value{
"darwin" on macOS, NULL on other platforms
}
\description{
Returns "darwin" for macOS, NULL for other platforms (Linux/Windows).
}
\details{
This is a specialized variant function for cases where only macOS
produces different results while Linux and Windows are identical.
Use this instead of \code{\link[=system_os]{system_os()}} when:
\itemize{
\item macOS produces unique output (e.g., platform-specific math libraries)
\item Linux and Windows produce identical results
\item You want to maintain a single snapshot for Linux/Windows
}

Common use cases:
\itemize{
\item JAGS MCMC output (macOS uses different floating-point arithmetic)
\item Platform-specific numerical computations
}
}
\examples{
\dontrun{
# Use with expect_snapshot_file when only macOS differs
expect_snapshot_object(
  mcmc_result, name = "test", variant = darwin_variant()
)
}
}
